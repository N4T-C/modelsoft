winget install Python.Python.3
python --version
pip --version
python -m ensurepip --upgrade


winget install -e --id Python.Python.3;

Write-Host "Restart PowerShell if python not detected";

python --version;
pip --version;

if (!(Get-Command pip -ErrorAction SilentlyContinue)) {
    python -m ensurepip --upgrade
}

python -m pip install --upgrade pip;

pip install requests;

python -m venv venv;

.\venv\Scripts\activate;

Write-Host "✅ Python, pip, venv READY"


git config --global user.name "Your Full Name"
git config --global user.email "your.email@example.com"
git status
git checkout -b feature-login
git log
git diff
git remote -v
git remote remove origin
git remote add origin https://gitlab.com/kuppanatc/temp.git

NORMAL PUSH (CLEAN SETUP)

rm -rf .git

git init

git add .

git commit -m "initial commit"

git branch -M main

git remote add origin https://github.com/your-username/your-repo.git

git push -u origin main


FORCE PUSH (DESTROY EVERYTHING ON REMOTE)

rm -rf .git

git init

git add .

git commit -m "force push reset"

git branch -M main

git remote add origin https://github.com/your-username/your-repo.git

git push -f origin main


GITLAB 

rm -rf .git

git init

git add .

git commit -m "force push reset"

git branch -M main

git remote add origin https://gitlab.com/kuppanatc/temp.git

git push -f origin main

git push --force --set-upstream origin main




